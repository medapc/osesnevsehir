# Cloudflare Pages Deployment Konfigürasyonu
# Bu dosyayı projenin root'una bırakın

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"

# Tüm sayfalar SSG ile pre-render edildiğinden
# server-side function gerekmez
